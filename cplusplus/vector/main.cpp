#include "vector.h"
#include <vector>

int main() {
    max::vector_test8();
    // std::vector<int> v(3);
    // v.resize(10, 7);
    // for (auto& i : v) {
    //     std::cout << i << std::endl;
    // }
    return 0;
}